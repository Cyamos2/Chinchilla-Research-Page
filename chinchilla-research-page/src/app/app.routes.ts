import { Routes } from '@angular/router';

// Define the routes for your Angular application
export const routes: Routes = [
  // Example route:
  // {
  //   path: 'example',
  //   component: ExampleComponent, // Import and assign the component to the route
  //   data: { title: 'Example Page' } // Additional data associated with the route
  // },

  // Add more routes as needed

  // Default route (redirect to a specific route or component)
  // { path: '', redirectTo: '/home', pathMatch: 'full' },
];

